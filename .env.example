# server/.env.example  (do not commit real secrets)
PROVIDER=groq                      # groq | openai
GROQ_API_KEY=                      # required if PROVIDER=groq
OPENAI_API_KEY=                    # required if PROVIDER=openai
MODEL_NAME=llama3-8b-8192          # groq default; for openai use gpt-4o-mini
SHARED_SECRET=change-me            # must match Roblox Config.EXTERNAL.SHARED_SECRET
FAST_REPLY_SECS=2.6
ATTEMPT_TIMEOUT_SECS=2.2
REQ_TIMEOUT_SECS=45
HOST=0.0.0.0
PORT=8000
